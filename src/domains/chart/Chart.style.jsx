import styled from "@emotion/styled";

const ChartContainer = styled.main``;

const ChartRealWrapper = styled.div`
  display: flex;
  flex-direction: column;
  align-items: space-between;
  padding-top: 30px;
`;

export { ChartContainer, ChartRealWrapper };
